import React from 'react';

const Header: React.FC = () => {
    return (
        <header style={{ padding: '10px', backgroundColor: '#f0f0f0' }}>
            <h1>Freezer Management System</h1>
        </header>
    );
};

export default Header;
